# 多语言国际化 (i18n) 设计文档

**日期**: 2026-08-18  
**状态**: 待审批  
**范围**: 前端 React + TypeScript 项目

---

## 目标

为 LearnHub 前端添加中英文多语言支持，用户可通过顶部导航栏切换语言，语言偏好持久化到 localStorage。

---

## 架构选型

### 技术方案：react-i18next + namespace 分模块

**理由**：
- react-i18next 是 React 生态最成熟的 i18n 库，API 简洁（`useTranslation` hook）
- namespace 分模块便于维护，300+ 字符串按功能域拆分
- 支持懒加载（后续可扩展）
- 社区活跃，类型安全

**依赖包**：
```json
"i18next": "^23.0.0",
"react-i18next": "^14.0.0",
"i18next-browser-languagedetector": "^7.0.0"
```

---

## 目录结构

```
src/frontend/src/i18n/
├── index.ts              # i18n 初始化配置
├── LanguageContext.tsx   # 语言切换组件（导航栏用）
└── locales/
    ├── en/
    │   ├── common.json   # 通用 UI 文案
    │   ├── auth.json     # 登录/注册
    │   ├── tutorials.json # 教程相关
    │   ├── admin.json    # 管理后台
    │   └── wizard.json   # 创建教程向导
    └── zh/
        ├── common.json
        ├── auth.json
        ├── tutorials.json
        ├── admin.json
        └── wizard.json
```

---

## Namespace 划分与关键 Key

### common.json（通用）
```json
{
  "login": "Login",
  "logout": "Logout",
  "register": "Register",
  "create": "Create",
  "back": "Back",
  "next": "Next",
  "loading": "Loading...",
  "confirm": "Confirm",
  "cancel": "Cancel",
  "save": "Save",
  "delete": "Delete",
  "edit": "Edit",
  "search": "Search",
  "close": "Close",
  "success": "Success",
  "failed": "Failed",
  "error": "Error",
  "language": "Language",
  "chinese": "中文",
  "english": "English",
  "switch_language": "Switch Language"
}
```

### auth.json（认证）
```json
{
  "title": "Welcome to LearnHub",
  "email_placeholder": "Enter your email",
  "password_placeholder": "Enter your password",
  "username_placeholder": "Choose a username",
  "forgot_password": "Forgot Password?",
  "create_account": "Create Account",
  "sign_in": "Sign In",
  "or_sign_in_with": "Or sign in with",
  "google": "Google",
  "github": "GitHub",
  "login_failed": "Login failed",
  "register_success": "Account created successfully!",
  "password_reset_sent": "Reset link sent to your email"
}
```

### tutorials.json（教程）
```json
{
  "my_tutorials": "My Tutorials",
  "public_tutorials": "Public Tutorials",
  "no_tutorials_yet": "No tutorials yet",
  "create_tutorial": "Create Tutorial",
  "generate_outline": "Generate Outline",
  "publish": "Publish",
  "unpublish": "Unpublish",
  "export": "Export",
  "export_pdf": "Export PDF",
  "export_markdown": "Export Markdown",
  "share": "Share",
  "like": "Like",
  "comments": "Comments",
  "chapter": "Chapter {{n}}",
  "chapter_status": {
    "pending": "Pending",
    "generating": "Generating...",
    "completed": "Completed",
    "failed": "Failed"
  }
}
```

### admin.json（管理后台）
```json
{
  "admin_panel": "Admin Panel",
  "users": "Users",
  "tutorials": "Tutorials",
  "dashboard": "Dashboard",
  "pending_review": "Pending Review",
  "approve": "Approve",
  "reject": "Reject",
  "make_admin": "Make Admin",
  "remove_admin": "Remove Admin",
  "total_users": "Total Users",
  "total_tutorials": "Total Tutorials"
}
```

### wizard.json（教程向导）
```json
{
  "step_1_title": "Step 1: Understand Your Learning Situation",
  "step_2_title": "Step 2: Configure Claude API",
  "step_3_title": "Step 3: Review and Modify Draft Outline",
  "step_4_title": "Step 4: Confirm Generation",
  "professional_level": "Your Programming Level",
  "learning_goal": "Learning Goal",
  "math_background": "Mathematics Background",
  "base_url": "API Base URL",
  "api_key": "API Key",
  "model_name": "Model Name",
  "start_generating": "Start Generating Tutorial",
  "outline_generated": "Generated Course Outline",
  "confirm_generation": "I confirm that I want to proceed with tutorial generation"
}
```

---

## 核心实现

### 1. i18n 初始化 (`src/i18n/index.ts`)

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

import commonEn from './locales/en/common.json';
import commonZh from './locales/zh/common.json';
// ... 其他 namespace

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en: { common: commonEn, auth: authEn, ... },
      zh: { common: commonZh, auth: authZh, ... }
    },
    fallbackLng: 'en',
    supportedLngs: ['en', 'zh'],
    interpolation: { escapeValue: false } // React 已处理 XSS
  });

export default i18n;
```

### 2. LanguageSwitcher 组件 (`src/i18n/LanguageSwitcher.tsx`)

```typescript
const LanguageSwitcher = () => {
  const { i18n } = useTranslation();
  const currentLang = i18n.language;
  
  const toggleLanguage = () => {
    const newLang = currentLang === 'en' ? 'zh' : 'en';
    i18n.changeLanguage(newLang);
    localStorage.setItem('language', newLang);
  };

  return (
    <button onClick={toggleLanguage} className="...">
      {currentLang === 'en' ? '🇨🇳 中文' : '🇬🇧 English'}
    </button>
  );
};
```

### 3. App.tsx 集成

```typescript
// 在 ThemeProvider 外层或同级添加 I18nextProvider（已由 i18n/index.ts 全局注册）
// 导航栏添加 LanguageSwitcher
```

### 4. 组件迁移策略

每个组件的字符串替换模式：

```typescript
// Before
<p>Loading tutorials...</p>

// After
import { useTranslation } from 'react-i18next';
const { t } = useTranslation('tutorials');
<p>{t('loading_tutorials')}</p>
```

---

## 影响范围

| 类型 | 数量 | 说明 |
|------|------|------|
| 新建文件 | 8 | i18n 配置 + 10 个翻译 JSON |
| 修改文件 | 24 | 10 pages + 14 components |
| 新增依赖 | 3 | i18next, react-i18next, i18next-browser-languagedetector |

---

## 实施步骤

1. 安装依赖
2. 创建 `src/i18n/` 目录和初始化配置
3. 创建翻译 JSON 文件（en + zh）
4. 创建 LanguageSwitcher 组件
5. 在 App.tsx 导航栏集成
6. 逐个组件替换硬编码字符串为 `t()` 调用
7. 测试中英文切换

---

## 成功标准

- [ ] 切换语言后，所有页面文字正确变更
- [ ] 语言偏好刷新后保持（localStorage）
- [ ] 浏览器语言自动检测生效
- [ ] 中英文翻译完整无遗漏
- [ ] 不影响现有功能（登录、教程生成、导出等）
